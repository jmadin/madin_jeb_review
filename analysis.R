# There are two component to the analysis, found in the R folder

# The code needs re-organizing. Probably best to move through each component bit by bit until this is done.

source("R/jeb_dislodgement_component.R")
source("R/jeb_energy_component.R")

